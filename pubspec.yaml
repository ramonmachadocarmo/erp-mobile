name: mobile
description: ERP mobile (Android / iOS).
publish_to: 'none'
version: 0.2.1+2

environment:
  sdk: ^3.11.1

dependencies:
  erp_schema:
    path: ../schema/generated/dart
  dio: ^5.11.0
  file_picker: ^11.0.3
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  flutter_secure_storage: 9.2.4
  go_router: ^17.5.0
  mobile_scanner: ^7.1.3
  url_launcher: ^6.3.2
  local_auth: ^3.0.2
  image_picker: ^1.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
